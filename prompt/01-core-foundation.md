# Prompt 01: Core Foundation

You are continuing the SalesPilot build. Start by reading `AGENTS.md`, `PROJECT_TRACKER.md`, and `plan/01-core-foundation.md`.

## Required Context

- Read every numbered document in `docs/` before changing code.
- Read relevant installed Next.js docs in `node_modules/next/dist/docs/` before adding routes, middleware, or framework-specific behavior.
- Confirm `PROJECT_TRACKER.md` marks `01-core-foundation` as active or blocked before proceeding.

## Goal

Implement or refine the core foundation plan: schema, auth facade, organization provisioning, app context, RBAC, API helpers, DTO mappers, and tests.

## Required Output

- Keep `plan/01-core-foundation.md` updated while working.
- Keep `PROJECT_TRACKER.md` updated after each meaningful step.
- Mark incomplete work `[~]` or `[!]` with an exact next action if context or limits run out.
- When complete, mark plan 01 `[x]` and open `prompt/02-auth-onboarding.md`.
