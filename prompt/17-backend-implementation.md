# Prompt 17: Backend Implementation

You are planning or implementing the backend wiring after the UI-first pass.

## Startup

- Read `AGENTS.md`.
- Read `PROJECT_TRACKER.md`.
- Confirm `plan/02-ui-first-pages.md` is `[x]`; if not, continue or unblock the UI-first plan before backend work.
- Read `plan/01-core-foundation.md`.
- Read the UI files created during plan 02 so backend DTOs and APIs support the visible interface.
- Read `docs/07-authentication.md`, `docs/08-rbac.md`, `docs/09-api-specification.md`, `docs/13-leads.md`, `docs/14-lead-forms.md`, `docs/21-rbac-implementation.md`, `docs/22-authentication-implementation.md`, `docs/23-organizations-multi-tenancy.md`, `docs/24-api-standards.md`, `docs/25-database-schema.md`, and `docs/28-coding-standards.md`.
- Read relevant installed Next.js docs before route, middleware, server action, or form work.

## Goal

Wire the completed UI to real authentication, onboarding, organization context, dashboard data, leads, members, forms, notifications, settings, validation, authorization, and API responses using the core foundation.

## Tracker Requirements

- Create the next backend implementation plan under `plan/`.
- Update `PROJECT_TRACKER.md` with active task, status, blockers, and next plan.
- Preserve all UI-first work unless backend integration requires a documented adjustment.
